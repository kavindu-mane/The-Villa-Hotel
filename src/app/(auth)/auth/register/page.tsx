import { Register } from "@/components";
import { Suspense } from "react";

const RegisterPage = () => {
  return (
    <Suspense>
      <Register />
    </Suspense>
  );
};

export default RegisterPage;
